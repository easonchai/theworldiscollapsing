services:
  db:
    image: postgres:17-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: twic
      POSTGRES_PASSWORD: twic
      POSTGRES_DB: twic
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:

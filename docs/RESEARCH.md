# Verified research — 2026-09-08

Facts checked against primary sources during planning. Re-verify anything marked ⚠ before relying on it. PRD and plan: issue #1.

## Video generation

| Fact | Value | Source |
|---|---|---|
| OpenRouter video generation | Live since 2026-04-15. `POST /api/v1/videos` → job id → `GET /api/v1/videos/{id}` → download. Async only. | openrouter.ai/docs/guides/overview/multimodal/video-generation |
| MiniMax video slugs on OpenRouter | `minimax/hailuo-3-max`, `minimax/hailuo-3`, `minimax/hailuo-2.3` (T2V + I2V) | openrouter.ai/minimax |
| MiniMax H3 Max price (direct) | 480P $0.05/s · 768P $0.08/s of output. 6s clip = $0.30 / $0.48 | platform.minimax.io/docs/guides/pricing-paygo |
| MiniMax H3 price (direct) | 768P $0.08/s · 2K $0.13/s | same |
| Clip length | H3 Max 5–15s, 480P/768P | platform.minimax.io/docs/guides/video-generation |
| Direct API | `https://api.minimax.io/v1/video_generation`, Bearer key, async + optional `callback_url` | platform.minimax.io/docs/api-reference/video-generation-i2v |
| ⚠ Concurrency | 15 concurrent jobs paid tier, 2 free — **third-party blog**, confirm in console | atlascloud.ai (secondary) |
| ⚠ OpenRouter markup over MiniMax | Not published on any fetched page | — |
| ⚠ Generation latency | ~15s for a 5s clip — from MiniMax *consumer* product page, not API docs | design.minimax.io/tools/minimax-h3-max |
| Continuous live video API | Does not exist commercially. Only research code (LiveAvatar, StreamDiT, LongLive) | github.com/Alibaba-Quark/LiveAvatar et al. |
| fal.ai alt | Hailuo-02 Standard $0.045/s (768p), Pro $0.08/s (1080p) | fal.ai/models/fal-ai/minimax/hailuo-02 |

Cost model used in plan: event = 10 first-half clips @480P + N branches × 10 clips @768P ≈ $6–9 (binary/3-way). ~100 events ≈ $600–900.

## LLMs (OpenRouter, prices per M tokens)

| Slug | Ctx | In | Out | seed | structured |
|---|---|---|---|---|---|
| `openai/gpt-6-astra` | 1.05M | $10 | $50 | yes | yes — **reasoning mandatory**, use for per-event authoring only |
| `openai/gpt-5-nano` | 400k | $0.05 | $0.40 | yes | yes — tickers/cards |
| `google/gemini-2.5-flash-lite` | 1M | $0.10 | $0.40 | yes | yes |
| `anthropic/claude-haiku-4.5` | 200k | $1 | $5 | **no** | yes |

- **Determinism is not available.** OpenRouter: "Determinism is not guaranteed for some models"; default routing load-balances across providers with different quantizations. Pin `provider.order` if you care. Do not build a "replayable sim" claim on this. (openrouter.ai/docs/api-reference/parameters)
- Structured outputs: `response_format: {type:"json_schema", strict:true}`; support is per-endpoint; set `require_parameters: true` to hard-fail non-supporting routes. (openrouter.ai/docs/guides/features/structured-outputs)
- ⚠ OpenRouter model pages are client-rendered; WebFetch hallucinated a model list once. Verify via `GET https://openrouter.ai/api/v1/models`.

## Randomness

| Fact | Value | Source |
|---|---|---|
| drand quicknet | Live since 2023-10. 3s rounds. Unchained BLS (G1, RFC 9380). Timelock-capable. | docs.drand.love/blog/2023/10/16/quicknet-is-live |
| drand HTTP | `https://api.drand.sh/v2/beacons/quicknet/rounds/{round}` | docs.drand.love |
| tlock | Encrypt to a future round; anyone decrypts after. `tlock-js` (TS), `tlock` (Go). Not needed for B-long; useful for sealing branches. | github.com/drand/tlock-js |
| On-chain verify | drand `evmnet` (BN254, EVM precompiles). `blocklock-solidity` / `blocklock-js` by Randamu. Stretch. | github.com/randa-mu/blocklock-solidity |
| `block.prevrandao` on L2 | **Unusable.** Arbitrum returns `1`; OP-stack (incl. Base) sequencer-settable. | specs.optimism.io/protocol/exec-engine |
| Chainlink VRF v2.5 | Live on Base + Base Sepolia. Result is public on fulfil → doesn't hide anything. ~2.5 LINK/request, minutes latency. **Rejected.** | docs.chain.link/vrf/v2-5/supported-networks |
| Commit-reveal | `keccak256(value, salt)`; proves unchanged-after-commit only. No OZ module. | chain.link/article/commit-and-reveal-schemes |
| Timelock-sealed prediction market in production | None found | — |

Outcome derivation (B-long): `outcome = uint(keccak256(sig_R ‖ eventId)) % n`, round R fixed on-chain at creation, `R` chosen after `lockTime + 10s`.

## Market mechanism

- Parimutuel: house is escrow, players pay players, pool ratio = live odds. No LP to drain. ~3–6h.
- AMM/LMSR under an information leak: LVR literature — "when all trades against the AMM are generated by informed arbitrageurs, liquidity provision exposes the LP exclusively to adverse selection." (paradigm.xyz/writing/pm-amm, arxiv.org/html/2607.17428). **Rejected.**
- LMSR max loss = `b·ln(n)` (arxiv 1206.5252). Gnosis LMSR uses `Fixed192x64Math` with overflow ceiling ~133·ONE. 2–4 days. Rejected.
- Reference parimutuel fork: `SivaramPg/evm-simple-prediction-market-contract` (MIT, ~680 LOC, binary only, `NoOpposition` revert instead of refund, dust stays in contract).
- Gnosis CTF (`gnosis/conditional-tokens-contracts`, LGPL) for split/merge if ever needed. Polymarket = hybrid CLOB (`Polymarket/ctf-exchange`), not applicable.

## Confidential compute

- **ZK adds nothing** over a hash commitment for "reveal matches commit." zkML (prove which model produced output) is open research. Dropped.
- **TEE does not hide prompts from the API you call.** TLS terminates at OpenAI/OpenRouter/MiniMax. Confidential *text* inference exists (Phala `inference.phala.com/v1`, NEAR AI Cloud); confidential *video* does not.
- Fastest on-chain-attested TEE: Oasis ROFL (5 CLI steps, native Sapphire attestation). Marlin Oyster has a literal "AI agent in TEE" tutorial. AWS Nitro has no network (vsock proxy needed). GCP/Azure produce OIDC/MAA tokens, no on-chain verifier.
- Under B-long the only real secret is the unreleased branch videos. tlock does that free; a TEE does it for the prize.

## Sponsor integrations (ETHOnline 2026, $80k, 11 sponsors)

| Sponsor | Base Sepolia | Gated | Status |
|---|---|---|---|
| The Graph — AI Tooling from-scratch $5k | `subgraphsSupportLevel: full` | No | **Build.** Subgraph MCP is the AI product: thegraph.com/docs/en/subgraphs/tooling/subgraph-mcp |
| Privy — financial flow $2.5k | In default chain list (84532), zero config | No | **Build.** `@privy-io/react-auth`, ~4 steps |
| World — Selfie Check $3.5k | Off-chain verify, no chain forced, standalone web OK (no mini-app) | **Yes** — beta feature flag + sandbox device request | Apply day 1. `@worldcoin/idkit` + `selfieCheckLegacy()` + `POST /v4/verify`. **Liveness/face-similarity, NOT age.** |
| Chainlink — Confidential Workflow $2k | Base Sepolia supported (CLI ≥1.0.0) | **Yes** — deploy access gated AND Confidential Workflows invite-only private beta | Apply day 1. `@chainlink/cre-sdk`, `handlerInTee`, `TeeRuntime`. Simulate locally without approval. |
| Hedera $15k, Arc $10k | other L1s | — | Rejected |
| 1inch $7k, Uniswap $5k | AMM tooling | — | Rejected (contradicts parimutuel) |
| ENS $5k | Ethereum Sepolia only | — | Rejected |
| Ledger $5k, Bazantic $3k | — | — | Rejected |

- No Base sponsor. No prediction-market track. No video sponsor. "prediction" appears 0 times in prize text.
- Event: **Sept 4–16, 2026**, async. ⚠ Submission cutoff not retrievable (event page HTTP 500) — check dashboard.
- ⚠ Ask ETHOnline Discord for sponsor fast-track on the two gated betas.

## Costs settled

- Video: ~$6–9/event → ~$600–900 for ~100 events.
- Authoring: GPT-6 Astra ≲$1/event.
- 24/7 continuous generation (rejected): $4,320/day at 480P per channel.
